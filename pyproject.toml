[project]
name = "mdadm-manager"
version = "1.49"
description = "Matrix-style graphical mdadm RAID manager for Linux"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[tool.uv]
package = false
python-preference = "only-system"
